import { VeilederData } from '../../rest/datatyper/veileder';
import { JSONValue } from 'yet-another-fetch-mock';

const veileder: VeilederData & JSONValue = {
	etternavn: 'Destructor',
	fornavn: 'The',
	ident: 'Z123456',
	navn: 'The Destructor'
};

export default veileder;
